# Інструкція з міграції та відновлення контексту

Цей файл містить інструкції для перенесення проекту в нову директорію та відновлення контексту роботи AI-асистента.

## 1. Як перенести проект (Міграція)

Оскільки ми вже зберегли всі важливі артефакти в папку `.ai-context` всередині проекту, процес міграції дуже простий:

1.  **Зупиніть сервер:** Натисніть `Ctrl+C` у терміналі, де запущено `npm run dev`.
2.  **Перемістіть папку:**
    - Візьміть всю папку `crystal-kuiper` з `c:\Users\yaros\.gemini\antigravity\playground\`.
    - Перемістіть її в `C:\Projects\`.
3.  **Відкрийте проект:**
    - Відкрийте VS Code (або Cursor).
    - `File` -> `Open Folder` -> Виберіть `C:\Projects\crystal-kuiper`.
4.  **Запустіть проект:**
    - Відкрийте термінал у VS Code.
    - Запустіть `npm install` (про всяк випадок, щоб оновити шляхи до залежностей).
    - Запустіть `npm run dev`.

---

## 2. Промпт для ПОЧАТКУ нової сесії

**Коли використовувати**: При відкритті нового чату (нова машина, новий день, або після перенесення проекту).

**Як почати новий чат в Antigravity/Cursor:**
- Натисніть `Ctrl+L` або `Cmd+L` (Mac) → Відкриється нова вкладка чату.
- Або: Кліком по "+" поруч з існуючими чатами.

**Скопіюйте цей промпт:**

```text
🚀 Відновлення контексту проекту "Crystal Kuiper"

Я продовжую роботу над SaaS Planner. Весь контекст проекту збережено в `.ai-context/`.

**Твої дії:**
1. Прочитай `.ai-context/PROJECT_CONTEXT.md` → Візія, архітектура, правила.
2. Прочитай `.ai-context/task.md` → Секція "Current Focus" (що робимо ЗАРАЗ) + Progress Tracker (що зроблено).
3. Скажи мені:
   - На якому етапі ми зупинилися?
   - Яке наступне завдання?
   - Чи потрібно щось додатково прочитати (`implementation_plan.md`, `walkthrough.md`)?

**НЕ читай** без запиту: `BACKLOG.md`, `audit_and_recovery.md`.

Жду короткого звіту про статус (2-3 речення).
```

---

## 3. Промпт для ЗАВЕРШЕННЯ сесії

**Коли використовувати**: Перед тим як закрити проект, перезавантажити ПК, або перейти на іншу машину.

**Скопіюйте цей промпт:**

```text
🛑 Завершення робочої сесії

Будь ласка, підготуй проект до збереження:

1. **Онови `.ai-context/task.md`:**
   - Відміть усі завершені завдання `[x]`.
   - Онови секцію "Current Focus" → вкажи наступну ціль.
   - Онови "Recent Decisions / Notes" → додай важливі рішення з цієї сесії.
   - Онови "Last Updated" → поточна дата/час.

2. **Опціонально (якщо були великі зміни):**
   - Онови `walkthrough.md` з новими змінами та як їх перевірити.
   - Додай в `BACKLOG.md` нові ідеї, які виникли під час роботи.

3. Коротко підсумуй (1-2 речення), що було зроблено за цю сесію.

Дякую! Після цього я зможу зробити `git commit` і продовжити роботу з іншого ПК.
```

---

## 4. Як економити токени (New Chat Strategy)

### Чому важливо починати новий чат?
- Довгі бесіди накопичують токени (весь історичний контекст).
- У великих проектах це може витрачати **50-100k токенів** на завантаження історії.

### Коли починати новий чат:
✅ **Кожного нового дня** (якщо проект великий).
✅ **Після завершення великої задачі** (наприклад, закінчили роботу з візардами → переходимо на API).
✅ **Після перезавантаження ПК** або зміни машини.

### Як це працює:
1. Ви завершуєте сесію **Промптом №3** (я оновлюю `.ai-context/task.md`).
2. Ви робите `git add .ai-context && git commit -m "Session checkpoint"`.
3. Наступного разу → **новий чат** → **Промпт №2** (я читаю тільки 2 файли замість 200k історії).

**Результат**: Замість 100k токенів історії, я читаю ~5k токенів з `.ai-context` файлів. ⚡
